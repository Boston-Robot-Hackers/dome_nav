# Chores

Running list of simple bug fixes and refactors (no spec/behavior change) that
don't warrant a feature/task pair. One line each; flip `- [ ]` to `- [x]` when
applied. A bug fix still gets a test.

## Todo

- [ ] Kilted compatibility for sim stack: (1) `nav2_params_explore_sim.yaml` fails on Kilted because `error_code_names` was replaced by `error_code_name_prefixes` (rejected at startup) and the default `cmd_vel` topic changed to `TwistStamped` while the Gazebo bridge still uses `Twist`. Applied to `nav2_params_explore_sim.yaml`: replaced `error_code_names` with the Kilted `error_code_name_prefixes` list and added `enable_stamped_cmd_vel: false` under `controller_server`, `velocity_smoother`, and `collision_monitor`. (2) Gazebo `/odom` was not bridged to ROS because better_launch maps `nav_msgs/Odometry` -> `gz.msgs.OdometryWithCovariance`, but Gazebo's diff-drive plugin on Kilted publishes `gz.msgs.Odometry`. Fixed in `launch/sim_robot.launch.py` by overriding the bridge's gazebo type to `gz.msgs.Odometry`. Other `nav2_params_*.yaml` files have the same Nav2 issue for Kilted real-robot/AMCL runs; fix them when those stacks are used on Kilted. (2026-07-29 F31 sim test)
- [ ] `nudge_toward_robot` direction: nudging straight toward the robot drags the goal into any wall between robot and frontier — the main generator of lethal candidates the F27 guard then rejects (reject-loop churn). Nudge along local free-space direction instead (e.g. toward the mean of the cluster's known-free neighbors). Behavior change — promote to a feature if it grows beyond a drop-in replacement. (2026-07-21 review #1)
- [ ] Novelty starvation: `best_frontier_candidates` returns one cell per cluster, so early exploration (one giant frontier ring) gives `pick_by_novelty` a single candidate — novelty is a no-op exactly when it matters most. Allow up to N spatially-separated cells per cluster. (2026-07-21 review #3; F15 live-verify TF15 T05 still pending too)
- [ ] Perf, blacklist loop in `best_cell_in_cluster`: per-cell × per-entry `math.sqrt` — compare squared distances instead. Bigger lever if needed: the full-map pure-Python scan in `find_frontier_clusters` at 1Hz is a real Pi CPU line-item (numpy or reduced tick rate). (2026-07-21 review #4)
- [ ] Sim A/B the post-nudge scoring change (score shift ~0.3m outward): confirm goal selection unchanged in ordering-only cases and sensible near preferred distance. (2026-07-21)
- [ ] mon21 real run used mixed sim/real params (`preferred_goal_distance 2.0, min_frontier_dist 0.5, min_frontier_size 10` — see emon2120-jul.json session_start): check which launch/config produced them and whether intentional. (2026-07-21)

- [ ] If FOLLOW/TF_ERROR persists on real robot after transform_tolerance bump, lower `controller_frequency` 20→10 Hz in `nav2_params_explore_real.yaml` to relieve Pi MPPI CPU load (keeps TF fresher).
- [ ] DRY param declaration via `dataclasses.fields()` (style-guide line 52): `declare_frontier_params` hand-lists every `FrontierParams` field twice (declare + read) and `merge_tuning` hand-copies each into `FrontierTuning` — each new param (F31 added 5) touches ~5-6 sites. Replace the declare/read hand-list with a `fields()` loop (`declare_dataclass_params(node, FrontierParams)`: declare each `f.name`/default, then `FrontierParams(**{f.name: get_parameter(f.name).value ...})`); reduce `merge_tuning`'s pass-through to a `fields()` iteration + explicit overlay of the 3 shared `ExploreParams` fields. Adding a field then auto-declares. Pure refactor, no behavior change; keeps the pure dataclass (no ROS leak into `frontier_explorer`/`frontier_algorithm`, unlike `generate_parameter_library`). Launch-signature repetition stays (better_launch AST-parses literals). Test: a fake node round-trips all fields through declare+read; `merge_tuning` output unchanged on fixtures. (2026-07-23 design discussion)
- [ ] Naming parallelism (F22 T01/R2): settle Explore vs Exploration across the seam — `ExploreParams`/`explore_tick`/`explore_algorithm`/`ExplorerManagerNode` (Explore) vs `ExplorationContext`/`ExplorationAlgorithm` (Exploration). Mechanical rename, no behavior change; update tests/imports. (Redundant `Pluggable` prefix already dropped from the node name.)

## Done

- [x] `tools/algo_demo.py` repaired for the F23/F31 param split — it constructed `ExploreParams(min_frontier_size=..., min_frontier_dist=..., goal_inset_m=...)` (fields moved to `FrontierParams`) and called `pick_best_frontier` with the pre-F31 keyword signature, so it died with `TypeError` on any run. Now: CLI args feed `FrontierParams`, `merge_tuning` builds the `FrontierTuning`, `find_frontier_clusters` gets `frontier_buffer_cells`, and `pick_best_frontier` gets `(tuning, blacklist=, data=)`; `blacklist_radius=max(0.5, inset+0.01)` guards the merge invariant. Demo now exercises the real F31 pipeline (clearance on by default). No pytest (standalone CLI viz tool; verified by `--auto` runs on room/maze/corridor incl. `--nudge-mode unknown`, all explore to completion). Literate `10-algo_demo.md` regenerated v1.1. (2026-07-24)
- [x] Comment cleanup per new style-guide rule (no multi-line narrative comment blocks): trimmed the 20-line `find_frontier_clusters` header to a docstring + `01-literate/06` pointer, and shortened the inner ring-walk / `best_frontier_candidates` blocks; tightened 9 five-line "why" blocks in `explorer_manager_node.py` (STUCK_T_S/MAX_GOAL_ATTEMPTS/WEDGED_STUCK_LIMIT constants, map_qos, TF listener, bounds/lethal guards, stuck/wedge/reselect/blacklist) to ≤2 lines — dated war-story detail lives in git/notes. No behavior change; full suite 256 passed. (2026-07-22)
- [x] `nav2_params_explore_real_mini.yaml`: local `cost_scaling_factor` 5.0→3.0 (back to upstream) — robot's driven trajectory hugged walls. Higher scaling = faster exponential cost decay = near-wall cells cheap to MPPI's CostCritic (weight 3.81 loses to path/goal pull). At 3.0 cost 0.3 m from wall rises ~132→170. The 5.0 was the 2026-07-20 anti-saturation tuning; the saturation fix was really the radius cut (0.70→0.4), so the scaling piece is safely revertable alone. If still too close, next lever local `inflation_radius` 0.4→0.55. No test (config; hardware retest pending). (2026-07-22)
- [x] `nav2_params_explore_real_mini.yaml`: header CHANGES table was stale vs in-place annotations — claimed local `inflation_radius` 0.15 (actual 0.4) and a global 0.5 that matches upstream 0.7 in the file. Synced per header rules 5/6; comments-stripped diff vs upstream verified clean. (2026-07-22)
- [x] Novelty visibility in telemetry: `session_params` now logs `use_novelty_scoring`/`novelty_top_n`; `goal_sent` events carry `telemetry_extra` (so `novelty_score` shows on goals, not only no_frontier ticks). Was impossible to confirm from telemetry whether a run had novelty on (tue22-3 run). Test `test_session_params_include_novelty_settings`. (2026-07-21)
- [x] `just_explorer.launch.py`: better_launch click wrapper crashes on `X | None` annotations — replaced None sentinels with -1/-1.0; bl also requires explicit values for every CLI arg (`--use_novelty_scoring true`). (2026-07-21)
- [x] `just_explorer.launch.py`: tuning params are now explicit launch args (min/max_frontier_dist, min_frontier_size, preferred_goal_distance, max_explore_radius, use_novelty_scoring, novelty_top_n) — better_launch silently drops undeclared CLI flags, so the header's "override any param at the CLI" claim was false (`--min_frontier_dist 0.5` never reached the node, 2026-07-21 tue22 runs). Unset args aren't passed → node defaults stay canonical; effective params printed at launch. No test (launch file; verified by launch-line log). (2026-07-21)
- [x] `goals_failed` counted watchdog failures too: `check_stuck`/`check_goal_timeout` now increment it — before, only Nav2-result failures counted, so a 27-goal session with 20 stucks reported `failed: 4` in session_end (20-jul telemetry). Tests `test_stuck/timeout_increments_goals_failed`. (2026-07-21)
- [x] Wedge detector: `note_stuck` — `WEDGED_STUCK_LIMIT` (3) consecutive stuck goals with same robot pose (≤ STUCK_MOVE_EPS) ⇒ log error, `wedged` telemetry event, dump exhaustion, stop session (idle). 20-jul telemetry: 12- and 20-goal same-pose stuck runs, ~21s each, ended only by manual kill. Streak resets on pose change or reached goal. Natural F29 escape trigger point. Tests `test_wedge_*`. (2026-07-21)
- [x] `on_goal_accepted` reject path: `clear_active_goal()` instead of bare `has_active_goal = False` — dangling `current_goal_xy`/`goal_start_time` showed the dead goal in /explore/status + markers until next tick. Test `test_rejected_goal_clears_all_active_state`. (2026-07-21)
- [x] `best_cell_in_cluster`: score the post-nudge goal — `abs((d - goal_inset_m) - preferred)` instead of raw `abs(d - preferred)`. Nudge pulls the dispatched goal exactly inset_m closer along the robot ray, so raw scoring biased selection ~0.3m too close to the robot. Behavior change is a constant shift (orderings for nearest/farthest unaffected). Test `test_pick_scores_post_nudge_distance`. (2026-07-21)
- [x] `merge_tuning`: validate `blacklist_radius > goal_inset_m` (raise ValueError) — exclusion coords are post-nudge, cluster cells raw; radius ≤ inset lets a rejected cell reselect every tick. Boundary validation per style guide. Tests `test_merge_tuning_rejects_radius_at_or_below_inset` / `_accepts_default_radius_and_inset`. (2026-07-21)
- [x] Drop the per-tick full-map copy: `find_and_send_goal` passes `m.data` (array.array) uncopied instead of `list(m.data)` — all consumers are read-only. `ExplorationContext.map_data` + `frontier_explorer` signatures relaxed `list[int]`→`Sequence[int]`. Test `test_map_data_accepts_uncopied_occupancy_grid_array`. (2026-07-21)
- [x] `explorer_manager_node.py` `on_goal_result`: stop blacklisting REACHED goals — blacklist moved into the failure branch only. Every success stamped a `blacklist_radius` (0.5m) dead circle over live frontier cells; accumulation over a session caused premature "done" (2026-07-10 exp-0004 finding). A persisting frontier now reselects, fails, and is blacklisted then. Tests `test_on_goal_result_reached_not_blacklisted` / `_failure_still_blacklisted`. (2026-07-21)
- [x] `frontier_explorer.py` `world_to_cell`: `int()` → `math.floor()` — `int()` truncates toward zero, so a point just left/below the map origin mapped to cell (0,0) (in-bounds) instead of (-1,-1), skewing `path_novelty_score` near the origin edge. Regression test `test_world_to_cell_floors_negative_coords`. (2026-07-21)

- [x] `nav2_params_explore_real_mini.yaml`: MPPI CPU cuts — `batch_size` 2000→1000, `time_steps` 56→36 (~2/3 less rollout compute). `controller_frequency` kept 20: tried 10 but MPPI requires controller period == `model_dt` (0.05), so freq 10 fatal-errored config ("Controller period more then model dt"); 10Hz would also need model_dt 0.1. NOTE the CPU-starvation premise is now in doubt — `top` during the errored/idle state showed the machine ~80% idle (~8 cores, 16GB), nothing pegged, so the 4-6Hz control loop may be single-thread MPPI saturation OR a TF/timing stall (`laser earlier than transform cache` / `queue is full`), not global CPU. Need per-core top DURING active nav to split the two. After the inflation fix the costmap was healthy (robot in free cell, goals free, no lethal storm, blacklist 2) but goals still aborted: the mon07 run showed `Control loop ... 4.2-6.9Hz` (wants 20) and `Timed out waiting for action server to acknowledge compute_path_to_pose` — pure Pi CPU starvation, plus MPPI too slow at 4-6Hz to execute a turn-around from a nose-to-wall pose. No test (config; hardware-only). If still CPU-bound, next lever is switching MPPI→Regulated Pure Pursuit. (2026-07-20)
- [x] `nav2_params_explore_real_mini.yaml`: `inflation_radius` 0.70→0.30 and `cost_scaling_factor` 3.0→5.0 on both local+global costmaps. RViz (SCR-20260720-pcyn, costmap color scheme) showed the cluttered post/wall space fully saturated with inflation at 0.70 — overlapping halos left almost no free (black) corridor between obstacles, so MPPI couldn't thread it, collision_monitor gated the footprint everywhere, and exploration wedged + spammed lethal-goal rejects. Diagnosis ruled out laser-TF miscalibration: obstacles rendered crisp (no smearing/doubling) and `/scan` points landed exactly on them. 0.30 leaves drivable gaps between obstacles; higher scaling keeps the inscribed ring tight. `robot_radius` kept 0.17 (deliberate post-crash tuning; 1cm negligible vs inflation). No test (config; hardware-only). (2026-07-20)
- [x] `frontier_params.py`: `FrontierParams.goal_inset_m` reverted 0.0→0.3. It had been dropped to 0.0 (disabling the `nudge_toward_robot` inward pull) on the theory the nudge was redundant with the F27 guard. The 2026-07-20 real-robot run (mon02e) **falsified that**: with inset 0.0 the frontier goal sits on the known/unknown boundary cell, which near a wall is inscribed (cost 99, `robot_radius` 0.17), so F27 correctly rejected a wall of ~8 goals every tick → infinite reselect loop → false "exploration done" with 108 clusters (incl. a 564-cell open frontier) unexplored. The nudge is load-bearing: pulling the goal 0.3 m toward the robot moves it off the inscribed ring into free space. It's the only source (no launch/yaml sets it — `robot_explore.launch.py:60`), so line 17 is the live value. No test (dataclass default; behavior exercised by F27 live re-run). (2026-07-20)
- [x] Deleted `launch/sim_explore.launch.py` — obsolete monolithic F13 full-sim launch, superseded by `sim_nav_full.launch.py` (composed from the split sim_* files; its header notes it exists to avoid sim_explore's duplicated logic). Nothing included it; not in current.md's Launch section. Removes one copy of the triplicated sim-defaults block (now duplicated across `sim_explore_node` + `sim_nav_full`). Also dropped current.md's stale next-step to delete the already-gone `sim_nav_default.launch.py`. No test (launch file removal). (2026-07-20)
- [x] `explorer_manager_node.py` `goal_is_lethal`: warn-log the lethal cell + its cost value when detected; dropped the caller's separate skip warn (now a single log owned by the guard). No behavior change. Covered by existing `goal_is_lethal`/reselect tests. (2026-07-20)
- [x] `explorer_manager_node.py`: `STUCK_T_S` 7→20 s so the explorer stops pre-empting Nav2's built-in recovery. Nav2's controller `progress_checker` (movement_time_allowance 10 s) now fails first → BT recovery (BackUp/Spin) runs before the explorer cancels + reselects. Still < `GOAL_TIMEOUT_S` 25. Diagnostic step: confirms whether built-in recovery can extract a wedged robot before we build a custom escape. No test (tuning constant). (2026-07-18)
- [x] `nav2_params_explore_real_mini.yaml`: crash-into-post tuning (2026-07-18) — `time_before_collision` 0.5→1.0 (0.5s braked collision_monitor too late); `robot_radius` 0.15→0.17 (true DOME 0.16 + ~1cm to cover flared post bases the lidar plane misses); MPPI `vx_max`/`vx_min` and velocity_smoother linear `max/min_velocity` 0.5→0.25 (slower explore). Header note updated. No test (config; hardware-only). Post's wider base is a sensing gap (2D scan can't see it) — radius padding is a stopgap; real fix is a low-obstacle sensor source / keepout layer (candidate feature).
- [x] `on_goal_result` stale-callback race (critique bugs #1/#3): a goal canceled by the stuck/timeout watchdog (`abandon_active_goal`→`clear_active_goal`) nulls `goal_start_time`, then the canceled goal's late result callback ran `time.monotonic() - None` → `TypeError` crash (seen live during F27 testing). Also would blacklist a goal canceled by `exploration_stop`. Fixed: `on_goal_result` early-returns when the callback is stale (`not has_active_goal`, `xy != current_goal_xy`, or `goal_start_time is None`). Regression tests `test_on_goal_result_ignores_cleared_goal` + `test_on_goal_result_ignores_superseded_goal`. Suite 231 passed.
- [x] Rename `goal_in_global_costmap` → `goal_within_costmap_bounds` (`explorer_manager_node.py`): the guard only checks the goal is inside the costmap rectangle (bounds/worldToMap protection), not that the cell is safe/traversable; old name oversold it. Pure rename, no behavior change; updated call site, tests, and literate. Lethal-cost check is separate (F27).
- [x] `explorer_manager_node.py` DRY/YAGNI pass (644→584 lines): extracted `dist()`/`rounded()` helpers, `call_hook()` (single optional-hook dispatch replacing 4 getattr wrappers), `abandon_active_goal()` (stuck/timeout cancel+blacklist+clear tail), `write_goal_result()` (3 duplicated telemetry writes). Dropped dead guards the state machine already guarantees (`last_progress_time`/`goal_start_time` None checks in the watchdogs), double algorithm-name validation (`resolve_algorithm` removed — `__init__` warns + falls back once at the param boundary), dead `start_xy` pre-capture in `on_intent` (tick owns it), unused `"succeeded"` in `GOAL_STATUS_NAMES`, duplicate `session_end` in `main()` (now only when state == exploring). Deleted `test_timeout_no_start_time_does_nothing` (encoded the dropped guard) and the 3 `resolve_algorithm` tests. Kept: malformed-JSON guard (true boundary), `paused_on_failure`/`exploration_resume` (resume is sent after stuck), costmap-None permissive default. Full suite 220 passed, 4 deselected.
- [x] `test_explorer_manager_node.py`: `test_unknown_explore_algorithm_param_falls_back_to_frontier` never injected an unknown `explore_algorithm` value (constructed the node with the default, so the warning assertion failed) — committed failing in the F22 close-out. Fixed by patching `Node.declare_parameter` to declare the param as `not_a_real_algorithm` during construction; full suite 211 passed, 4 deselected.
- [x] `frontier_explorer.py`: `find_frontier_clusters` clamped `max(1, buffer_cells)`, silently ignoring `buffer_cells=0`. Now 0 means boundary cells (touching unknown); added regression test.
- [x] `nav2_params_explore_real.yaml`: raise MPPI `FollowPath.transform_tolerance` 0.1→0.3 — Pi TF stale >0.1s under MPPI load caused FOLLOW/TF_ERROR aborting goals in 0.4s (real-robot run 2026-07-13).
